# MCP SDK — matches existing dex pattern
mcp>=1.0.0,<2.0.0

# Environment variable loading
python-dotenv>=1.0.0

# HTTP client for Meta Graph API
requests>=2.31.0

# Google Ads
google-ads>=24.0.0
google-auth-oauthlib>=1.2.0

# Google Sheets (trafficking sheet integration)
google-api-python-client>=2.100.0
google-auth>=2.23.0

# Corporate TLS proxy support (Zscaler, Netskope, Cloudflare WARP, etc.)
truststore>=0.9.1
